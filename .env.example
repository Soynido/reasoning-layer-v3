# RL3 LLM Bridge Configuration
# 
# Optional: Enable semantic natural language understanding
# The system works perfectly WITHOUT these (offline mode with pattern matching)
# 
# Choose ONE provider:

# Option 1: Anthropic Claude (recommended - fast & cheap)
# ANTHROPIC_API_KEY=sk-ant-...

# Option 2: OpenAI GPT
# OPENAI_API_KEY=sk-proj-...

# Cost analysis:
# - Anthropic Claude Haiku: ~$0.0003 per intent (~$5-10/month heavy usage)
# - OpenAI GPT-3.5 Turbo: ~$0.0005 per intent (~$10-15/month heavy usage)
# - LLM only called when offline confidence < 0.7 (~30% of requests)
#
# Without API key: System uses pattern matching (works great for common phrases)
# With API key: System understands flexible phrasing, verb conjugations, complex queries
