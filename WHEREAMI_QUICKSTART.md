# 🚀 WhereAmI Snapshot — Quick Start Guide

## Test immédiat (2 minutes)

### 1. **Rebuild l'extension**

```bash
npm run compile
```

### 2. **Recharger la fenêtre VS Code**

- Appuyez sur `Cmd+Shift+P` (Mac) ou `Ctrl+Shift+P` (Windows/Linux)
- Tapez "Developer: Reload Window"
- Appuyez sur Entrée

### 3. **Exécuter la commande**

- Appuyez sur `Cmd+Shift+P` (Mac) ou `Ctrl+Shift+P` (Windows/Linux)
- Tapez "Where Am I"
- Sélectionnez "🧠 Where Am I? — Cognitive Snapshot"

### 4. **Voir le résultat**

Un nouveau fichier Markdown temporaire s'ouvre avec le snapshot cognitif actuel :

```markdown
# 🧠 Where Am I? — RL4 Cognitive Snapshot

**Generated at**: mardi 11 novembre 2025 à 14:45:32  
**Cycle**: 42  

---

## 📍 Current Context

Currently focused on: `extension/kernel/api/WhereAmISnapshot.ts`

**Recently viewed**:
- `extension/extension.ts`
- `package.json`
- `extension/kernel/CognitiveScheduler.ts`

---

## 🔍 Cognitive Patterns

- **pattern-config-mutation** (85% confidence, 📈 increasing)
- **pattern-kernel-stabilization** (72% confidence, ➡️ stable)

---

## 📈 Forecasts

- *New ADR proposal for API refactor* (78% confidence)
- *Increased test coverage needed* (65% confidence)

---

## 💡 Recommendations

- Resume editing **extension/kernel/api/WhereAmISnapshot.ts** to continue the current reasoning path.
- Check recent patterns for alignment with this file.
- 2 high-confidence pattern(s) detected — leverage these insights.

---

_Automatically generated by RL4 Cognitive Replay_
```

---

## Test programmatique (optionnel)

### Utiliser l'API directement

```typescript
import { generateWhereAmI, generateSnapshotJSON } from './extension/kernel/api/WhereAmISnapshot';

// Générer snapshot Markdown
const markdown = await generateWhereAmI();
console.log(markdown);

// Générer snapshot JSON
const snapshot = await generateSnapshotJSON();
console.log(JSON.stringify(snapshot, null, 2));
```

### Exécuter les tests

```bash
npm run test:whereami
```

**Résultat attendu** :
```
🚀 Running WhereAmI Snapshot Tests...

🧪 Test 1: Snapshot Generation
✅ Test 1 passed: Markdown snapshot generated successfully

🧪 Test 2: JSON Snapshot Generation
✅ Test 2 passed: JSON snapshot generated successfully
📊 Snapshot data: {...}

🧪 Test 3: Empty Data Handling
✅ Test 3 passed: Empty data handled gracefully

✅ All tests passed!
```

---

## Que faire si ça ne marche pas ?

### Problème 1 : "Command not found"
**Solution** : Rechargez la fenêtre VS Code (`Developer: Reload Window`)

### Problème 2 : Snapshot vide ou incomplet
**Causes possibles** :
- RL4 Kernel pas encore activé → Attendez 3-5 secondes après l'ouverture
- Pas de données RL4 → Exécutez quelques cycles manuellement (`RL4 Kernel: 🧠 Run Cognitive Cycle`)
- Fichiers `.reasoning_rl4/` manquants → Vérifiez que le dossier existe

**Solution** :
```bash
# Vérifier que RL4 est actif
ls -la .reasoning_rl4/

# Devrait contenir :
# - ledger/cycles.jsonl
# - traces/ide_activity.jsonl
# - patterns.json
# - forecasts.json
```

### Problème 3 : Erreur TypeScript
**Solution** :
```bash
# Recompiler l'extension
npm run compile

# Vérifier les lints
npm run lint
```

---

## Documentation complète

- **API Reference** : `docs/WHEREAMI_SNAPSHOT_API.md`
- **WebView Integration** : `docs/WHEREAMI_WEBVIEW_INTEGRATION.md`
- **Implementation Report** : `WHEREAMI_IMPLEMENTATION_COMPLETE.md`

---

## Feedback

Si vous rencontrez un problème ou avez une suggestion :

1. Ouvrez une issue GitHub
2. Incluez le snapshot Markdown généré
3. Incluez les logs RL4 Kernel (Output Channel)

---

**Bon test ! 🚀**

